
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: backuppools.jenkins.io
spec:
  group: jenkins.io
  names:
    kind: BackupPool
    listKind: BackupPoolList
    plural: backuppools
    singular: backuppool
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: BackupPool is the Schema for the backuppools API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: BackupPoolSpec defines the desired state of BackupPool
          properties:
            type:
              description: Type describes the type of backupPool does one want "unified"
                creates a single PVC and all backups are stored in the corresponding
                PV denoted by the Backup name "open" creates a new PVC for each single
                Backup instance
              type: string
          required:
          - type
          type: object
        status:
          properties:
            jenkinsRef:
              type: string
            persistentVolumeClaimRef:
              type: string
          required:
          - jenkinsRef
          type: object
      type: object
  version: v1alpha2
  versions:
  - name: v1alpha2
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
